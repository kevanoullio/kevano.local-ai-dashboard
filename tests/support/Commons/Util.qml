pragma Singleton
import QtQuick

QtObject {
  // Test stub: launching things is out of scope for tests, so these never run.
  function execArgv(args) { return false }
  function exec(command) { return false }
}